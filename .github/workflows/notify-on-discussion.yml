name: notify-on-discussion

on:
  discussion:
    types: [created, edited, answered]
  discussion_comment:
    types: [created, edited]
  push:

jobs:
  new-task:
    uses: justinjbird/workflow/.github/workflows/message-teams.yml@main
    with:
      message: New comment on discussion https://github.com/justinjbird/site-blog-comments/discussions
